<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileAppsCoursework"
        tools:targetApi="31">
        <activity
            android:name=".LoginPageActivity"
            android:exported="true"
            android:theme="@style/Theme.MobileAppsCourseworkLogin"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.MobileAppsCourseworkLogin"
            android:label="Home">
        </activity>
        <activity android:name=".SignUpPageActivity"
            android:exported="true"
            android:theme="@style/Theme.MobileAppsCourseworkLogin"
            android:label="Sign in page">
        </activity>
        <activity android:name=".LoggingChunksActivity"
            android:exported="true"
            android:theme="@style/Theme.MobileAppsCourseworkLogin"
            android:label="Record your activity">
        </activity>
    </application>

</manifest>